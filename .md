I am building an ATS/CRM system using Next.js (App Router) and React. I want to build an "AI Resume Auto-fill" feature for my "Add Candidate" page.

Here is the exact workflow and technical constraint I want to follow:
1. FRONTEND: Add a file upload input (for PDFs) above my candidate form. When a file is selected, show a smart multi-step loader (e.g., "📄 Uploading...", "🔍 Scanning...", "✨ Extracting Data...").
2. BACKEND (Next.js API Route): Receive the PDF file. CRITICAL: Do NOT save the file to the disk or MongoDB. Keep it in the server's RAM (memory) only.
3. PDF PARSING: Use the `pdf-parse` npm package to extract raw text from the in-memory PDF buffer.
4. AI EXTRACTION: Send the raw text to the Google Gemini 1.5 Flash API. Instruct the AI to extract specific details and return ONLY a valid JSON object.
5. AUTO-FILL: Send the JSON back to the frontend to automatically update my React `formData` state. Discard the file from memory.

My form state has the following fields that need to be extracted:
name, phone, email, address, age, qualification, specialization, expertise, experience, lastSalary, expectedSalary.

Please provide:
1. The Next.js API route code (`/api/parse-resume/route.js`) using `pdf-parse` and `@google/genai` (Gemini API).
2. The Frontend React code (button and logic) to handle the file upload, the smart loading state, and the auto-filling of the `formData` state.